# Huong dan cai dat

## Moi truong

1. Tao va kich hoat moi truong ao Python.
2. Cai dependencies:

```bash
pip install -r requirements.txt
```

## Render scene

```bash
manim -pqh scenes/part1_pixel_space.py PixelSpaceScene
```

Thay `PixelSpaceScene` bang ten scene tuong ung khi render cac phan khac.
